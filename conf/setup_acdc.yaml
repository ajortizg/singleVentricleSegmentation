data:
  # Input data dir
  # Use only training split becase there are no groundtruth masks avaibale for the testing split 
  raw_dir: 'data/acdc/training'
  # Output data dirs
  save_dir: 'results'
  out_imgs_dir: 'NIFTI_4D_Datasets'
  out_segs_dir: 'NIFTI_Single_Ventricle_Segmentations'
  out_metadata_file: 'Segmentation_volumes.xlsx'

# 'right-ventricle':  1
# 'myocardium':       2
# 'left-ventricle':   3
label: 
  roi: 'myocardium'
  tag: 2

debug:
  viz: true
  alpha: 0.3
  color:
    - 0
    - 1
    - 1
  aspect_ratio: 1.7
  gif: true
  dur: 200